apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: wanderbuddy
  labels:
    app: api
spec:
  type: ClusterIP
  ports:
    - port: 4000
      targetPort: 4000
      protocol: TCP
  selector:
    app: api
