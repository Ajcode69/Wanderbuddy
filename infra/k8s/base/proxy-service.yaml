apiVersion: v1
kind: Service
metadata:
  name: proxy
  namespace: wanderbuddy
  labels:
    app: proxy
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
  selector:
    app: proxy
