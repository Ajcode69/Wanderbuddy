apiVersion: v1
kind: Service
metadata:
  name: agent
  namespace: wanderbuddy
  labels:
    app: agent
spec:
  type: ClusterIP
  ports:
    - port: 4100
      targetPort: 4100
      protocol: TCP
  selector:
    app: agent
